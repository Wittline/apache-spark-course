{
    "applicationConfiguration": [
        {
            "classification": "hive-site",
            "configurations": [],
            "properties": {
                "hive.exec.scratchdir": "s3://racv-emr-serverless-bucket/hive/scratch",
                "hive.metastore.warehouse.dir": "s3://racv-emr-serverless-bucket/hive/warehouse"
            }
        }
    ],
    "monitoringConfiguration": {
        "s3MonitoringConfiguration": {
            "logUri": "s3://racv-emr-serverless-bucket/logs/"
        }
    }
}
